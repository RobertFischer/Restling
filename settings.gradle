include 'core', 'test', 'test:wars'

new File(rootDir, "test/wars").eachDir { dir ->
  if(dir.name == "build") return
  System.out.println("Including $dir")
  include "test:wars:$dir.name"
}

rootProject.children.each { project ->
  project.name = "restling-$project.name"
}

